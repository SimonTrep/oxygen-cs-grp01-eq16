[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests = "*"
signalrcore = "*"
python-dotenv = "*"
pyodbc = "*"

[dev-packages]

[requires]
python_version = "3.8"

[scripts]
start = "python src/main.py"
test = "python -m unittest discover"
lint = "pylint -rn -sn --rcfile=.pylintrc"
